package state;

// StateEnum defines the various states the application can be in
public enum StateEnum {
	DISPLAY_TIME, // S1: State for displaying the current time
	DISPLAY_DATE, // S2: State for displaying the current date
	CHANGE_TIME,  // S3: State for changing the current time
	CHANGE_DATE   // S4: State for changing the current date
}
