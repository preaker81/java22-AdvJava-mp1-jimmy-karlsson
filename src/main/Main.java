package main;

public class Main {

    // The main method serves as the entry point for the Java application
	public static void main(String[] args) {
        // Create a new instance of the ClockApplication class
		ClockApplication clockApplication = new ClockApplication();
        
        // Call the run method of the ClockApplication instance to start the application
		clockApplication.run();
	}
}
